# KVM-AntiAntiVM Ubuntu 24.04 離線部署版

本資料夾可複製到乾淨的 Ubuntu 24.04 amd64 主機使用，不依賴 Fedora/RPM 流程。

## 第一次啟動

1. 將整個資料夾複製到目標電腦，不要只複製 `deploy.sh`。
2. 進入此資料夾。
3. 先驗證離線資源，再啟動選單。

```bash
cd KVM-AntiAntiVM-部署版
./deploy.sh validate-offline
sudo ./deploy.sh
```

主選單先執行第 1 項「Ubuntu 主機一鍵部署／更新」，依提示重新開機後再建立 VM。

## VM 建議順序

1. 建立新 Windows VM，磁碟 GiB 是第一個輸入項目。
2. 完成 Windows 安裝並完整關機，再執行「去虛擬化」。
3. 首次「一鍵直通」保留 VNC/VGA，讓 Windows Update 或使用者安裝適用的 GPU 驅動。
4. 驅動完成後關機，再執行一次「一鍵直通」，切換成純實體 GPU 輸出。

維護模式與正式直通均為硬體通用流程，不內建 NVIDIA、AMD 或 Intel 特定驅動。

## 刻意未包含

- Windows ISO：請使用自己合法取得的 ISO，建立 VM 時輸入其絕對路徑。
- 顯示卡廠商驅動：由 Windows Update 自動取得，或由使用者自行準備。
- 破解軟體、本機 VM、測試截圖、暫存建置檔和個人設定。

`KVM/ubuntu-24.04.3-desktop-amd64.iso` 已保留，供 Ubuntu 安裝或救援使用。

## 完整文件與驗證

詳細說明請讀 `DEPLOYMENT.zh-TW.md`。任何檔案傳輸完成後都可重新驗證：

```bash
./deploy.sh validate-offline
```

